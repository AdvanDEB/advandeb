# AdvanDEB — Change Plan, 2026-08-26

Supersedes `docs/REVIEW-2026-05-18.md` §11.

That plan had 36 numbered items. **26 of them are done.** This document
carries forward only what survived an item-by-item audit against the tree at
`cb7dd3c1`, plus what the audit newly surfaced. Nothing here is inherited on
faith — every open item below was confirmed open by reading the current code
or querying the live datastores on 2026-08-26.

---

## 1. Audit result of the May plan

### Closed (26)

**P0:** JWT type confusion (1) · `/health` shadow (2) · default
`JWT_SECRET_KEY` rejection (3) · Google-auth exception leak (4) · working
tree committed (7).

**P1:** SEO basics (9) · OAuth `?code=` reload trap (11) · stray
`console.log`s (12).

**P2:** `datetime.utcnow()` — 96 sites → **0** (13) · constant-time login
(15) · `get_current_user` ObjectId guard (16) · hard-coded chatbot WS URL
(17) · real CI (20) · `app/README.md` refresh (21) ·
`knowledge-builder/README.md` refresh (22).

**P3:** `graph_query_service.py` 1,625 → 381 lines, split into
`graph_query_fetchers` / `graph_query_layouts` / `graph_query_serialization`
(25, the one godfile genuinely dealt with) · real `/logout`
with `jti` revocation + TTL (29) · `slowapi` rate limiting on auth endpoints
(30) · MCP gateway SIGTERM graceful shutdown (31) · MCP bound to loopback
(32) · Playwright harness exists (33, partial — see below).

### Still open, carried forward

Numbered `C1…` below. Old item numbers in parentheses.

---

## 2. P0 — Correctness we are currently blind to

### C1 — The CI knowledge-builder job tests nothing *(new)*

`.github/workflows/ci.yml`:

```yaml
- name: Pytest -- knowledge-builder
  working-directory: knowledge-builder
  run: pytest -x -q tests/ || true
```

Two independent faults, and they mask each other:

1. `|| true` swallows every failure.
2. It runs `knowledge-builder/tests/`, but the KB test module that is
   actually broken lives in the **repo-root** `tests/` directory — so CI
   never even loads it.

**Fix:** drop `|| true`; add the repo-root `tests/` directory to the job (or
consolidate the two test roots — see C2). Accept only when a deliberately
broken test causes a red build.

**Why P0:** every other quality claim in this document rests on CI being
honest. It currently is not.

### C2 — `tests/test_layout_algorithms.py` does not import *(5)*

```
ModuleNotFoundError: No module named 'advandeb_kb.services.visualization_service'
```

Same failure class the May review flagged for `test_graph_service.py` — a
test outliving the module it covers. The layout code now lives in
`graph_query_layouts.py` (part of the `graph_query_service` split).

**Fix:** rewrite the module against `graph_query_layouts`, or delete it if
the layout algorithms are covered elsewhere. Decide which; do not leave it.

**Note:** there are two test roots (`tests/` and `knowledge-builder/tests/`)
with no stated division of labour. Worth collapsing while fixing C1.

### C3 — `provenance_traces` is empty *(new)*

Arango `provenance_traces` holds **0 documents**, yet `provenance_service.py`
exists in the backend and the provenance trail panel is listed as shipped.

Either provenance is persisted somewhere else (in which case the empty
collection is dead schema and should be dropped), or the trail panel is
rendering something reconstructed on the fly and nothing is durable. These
have very different implications for a system whose founding goal was
"eliminate hallucinations … with full provenance tracking."

**Fix:** determine which. This is an investigation, not a code change —
timebox it, then file the real item.

### C4 — `tokenUrl` points at a non-existent endpoint *(6, 14)*

`app/backend/app/core/auth.py:15`:

```python
oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/api/auth/token")
```

`/api/auth/token` does not exist. Cosmetic in effect — it only mis-renders
the OpenAPI "Authorize" dialog — but it is a two-minute fix that has now
survived two review cycles.

**Fix:** point at `/api/auth/login`.

---

## 3. P1 — The public face

### C5 — No landing page above the auth wall *(8)*

`HomeView.vue` opens with `v-if="authStore.isAuthenticated"` and has no
unauthenticated branch. An anonymous visitor to advandeb.com gets the login
wall and no explanation of what the project is.

The SEO work (9) is done — description, OpenGraph, Twitter card,
`robots.txt`, `sitemap.xml`, `og-image.png` all shipped. **That work is
currently pointed at an empty room.** A crawler follows the metadata to a
page with no content.

**Fix** (content task more than engineering):
- One-paragraph mission statement.
- A static screenshot of the graph with a caption.
- Three-step "How it works": ingest → extract → review/explore.
- Links: docs, contact, and the institution behind the project.
- Footer with imprint and licence.

Real numbers are now available and worth putting on the page: 1,253 curated
papers, 1,236 stylized facts, 31,311 support edges, a 1.26M-node taxonomy.

### C6 — No anonymous read-only path *(10, 34)*

`SYSTEM-OVERVIEW.md` describes a `Knowledge Explorator` role with read-only
access to published knowledge. In the code, `"anonymous"` appears only as a
websocket session-id fallback (`api/routes/ws.py`). There is no anonymous
role and no unauthenticated read path.

This is the **strategic decision of the quarter**, not a task. Two coherent
answers:

- **(a) Commit to public.** An `"anonymous"` virtual role with
  `Knowledge Explorator` capabilities and a hard read-only guard. Requires
  designing for caching, indexing, and abuse handling against a corpus this
  size — non-trivial at 3.9M documents.
- **(b) Commit to closed.** Remove the read-only role from the documentation
  and stop implying a public KB.

Doing neither is the current state and is the worst of the three. Decide
before building C5, because the answer changes what the landing page says.

### C7 — Google sign-in is hidden with no stated end-state *(new)*

`ed3d21d6` — "temporarily hide the Google sign-in option." Temporary states
need an expiry. Either finish the OAuth configuration and re-enable, or
remove the path and drop `GOOGLE_*` from config and `.env.example`.

---

## 4. P2 — Documentation that matches reality

### C8 — `RUNNING.md` omits two of the three datastores *(new)*

The architecture block in `RUNNING.md` lists MongoDB and Ollama. It does not
mention **ArangoDB** (canonical KB store, 12.1M documents across all
collections) or **ChromaDB** (17 GB of vectors). The prerequisites section
tells a new operator to have Mongo and Ollama running — follow it exactly and
the retrieval stack does not work.

**Fix:** add both to the topology diagram and the prerequisites, including
the `ARANGO_URL` / `ARANGO_DB_NAME` / `ARANGO_USERNAME` / `ARANGO_PASSWORD`
env vars and the `data/chromadb` path and its disk requirement.

### C9 — The storage contract is unwritten *(24)*

Three stores and two Mongo databases hold overlapping entities:

| Entity | Mongo `advandeb` | Mongo `..._kb` | Arango `advandeb_kb` |
|---|---|---|---|
| documents | 1,695 | 1,305 | 3,899,789 |
| facts | 28,309 | 63,709 | 109,395 |
| stylized_facts | 1,236 | 1,236 | 1,236 |
| graph nodes | 49,700 | 102,060 | — |
| graph edges | 70,724 | 155,397 | 31,311 (`sf_support`) |
| taxonomy | 1,258,523 | 1,257,912 | 1,257,912 |

The numbers disagree in every row but one. Some of this is certainly
intentional (curated app-facing subset vs. full KB), but none of it is
written down, and the two `Settings` objects (`app` and `advandeb_kb`) make
it worse by giving each half its own configuration surface.

**Fix:** a short `docs/STORAGE-CONTRACT.md` — for each entity, the
authoritative store, the derived copies, the sync trigger, and the staleness
tolerance. Then reconcile the two `Settings` objects against it.

**Why this matters more than it looks:** every future "why does the graph
show X but chat says Y" bug will start here.

### C10 — Roadmap phase reconciliation *(23)*

Done in this revision — `docs/ROADMAP.md` is re-anchored to 2026-08-26 with
a current-quarter section. Listed for completeness.

---

## 5. P3 — Architecture

### C11 — Three of four godfiles grew instead of splitting *(25)*

| File | May | Now | Δ |
|---|---|---:|---|
| `advandeb_kb/services/graph_query_service.py` | 1,625 | **381** | **−1,244 — split, done** |
| `app/backend/app/kb/pipeline.py` | 1,523 | **1,662** | +139 |
| `advandeb_kb/agents/chatbot_agent.py` | 946 | **1,205** | +259 |
| `advandeb_kb/agents/react_engine.py` | 952 | **1,061** | +109 |

Only `graph_query_service.py` was actually split — into `graph_query_fetchers`
/ `graph_query_layouts` / `graph_query_serialization`. The other three all
grew. Note the path correction: the May plan wrote `app/kb/pipeline.py`; the
file is at `app/backend/app/kb/pipeline.py` and was never split.

**Fix:** the original prescriptions still hold — `pipeline.py` →
`pipeline/{concurrency,pdf,kg_link}.py`; the two agents → separate transport
from policy. Not urgent, but three of four moving the wrong way is a trend,
and each month of growth makes it more expensive.

### C12 — Playwright covers two of five flows *(33)*

`login.spec.ts` and `chat.spec.ts` exist. The May plan asked for login,
upload, KB graph load, chat with citation expansion, and provenance trail.

**Fix:** add the three missing specs. Graph load is the highest-value one —
it exercises Arango, the artifact store, and the Cosmograph canvas together,
and it is the flow with no unit coverage at all.

### C13 — `_is_primary_worker()` heuristic *(27)*

`app/backend/app/main.py:65`. Singleton task election still uses a heuristic
rather than a MongoDB lease. Only bites when the backend runs multiple
workers — worth knowing before that day, not after.

### C14 — Token storage hardening *(28)*

Access token in memory, refresh token in an httpOnly + Secure +
SameSite=Strict cookie, with the matching CSP documented. Unchanged from May;
still open. Lower priority now that `/logout` revocation and rate limiting
have landed, but the XSS exposure is real.

### C15 — Async ArangoDB driver / thread-pool audit *(26)*

`HybridRetrievalService` runs sync Arango calls through a 4-worker
`ThreadPoolExecutor`. At 3.9M documents this is a plausible throughput
ceiling. Measure before rewriting — this may be fine, and the measurement is
cheap.

### C16 — Open-source decision *(35)*

READMEs reference a fork-and-PR workflow with no public repo. Same class of
problem as C6: the documentation promises a posture the project has not
chosen. Pairs naturally with the C6 decision.

### C18 — Finish or formally abandon the Mongo→Arango migration *(new)*

`app/backend/app/core/config.py:61`:

```python
# Transitional: KB_DB_NAME used during MongoDB→ArangoDB migration. Remove after migration.
KB_DB_NAME: str = "advandeb_knowledge_builder_kb"
```

That comment is the explanation for every count mismatch in C9. The
`advandeb_knowledge_builder_kb` database is the **previous generation** of the
knowledge store, mid-migration to ArangoDB — and it is still written to by the
ingestion workflow, the visualization artifact store, and user submissions.

`core/database.py:17` describes it as "ingestion workflow state only." That is
no longer accurate.

**This is the largest untracked piece of work in the system.** It is not on the
old plan at all, because in May the migration was in flight (`b1999819`,
"pre-review baseline (Mongo→Arango migration WIP)"). It has now been in flight
for three months, and the half-state is what makes the system hard to reason
about.

**Fix:** two honest options.
- **Complete the cutover** — migrate the remaining writers, drop the database,
  remove `KB_DB_NAME`, collapse the two `Settings` objects.
- **Formally promote it** — give it a documented role and an owner, and rewrite
  the comment. Only defensible if there is a reason it must stay.

Doing neither is the current state, and it compounds: every new feature that
touches ingestion or visualization has to pick a database, and there is no rule
to pick by beyond `docs/STORAGE-CONTRACT.md`, which describes the mess rather
than resolving it.

### C17 — Design system *(36)*

Element Plus and Tailwind tokens coexist with hand-rolled hex codes in
`LoginView`/`HomeView`. Pick one. Best done immediately before C5, since the
landing page is new surface that will otherwise add a fourth style.

---

## 6. Suggested order

**This week — restore trust in the tooling.** C1, C2, C4. Half a day
together, and C1 is the precondition for believing any future green build.
Then C3 as a timeboxed investigation, because the answer may reframe how the
provenance story gets told publicly.

**This month — decide, then build the front door.** C6 first as a decision
(it is a fork in the road, and C5's copy depends on it), then C17, then C5.
Shipping the landing page without the C6 answer means writing marketing copy
you may have to retract.

**Alongside — write down what exists.** C8 and C9 — **both done as of
2026-08-26** (`RUNNING.md` reconciled, `docs/STORAGE-CONTRACT.md` written).

**Then decide the migration — C18.** This is the biggest item on the list and
the one nothing else can route around. Every ingestion or visualization change
made before it is resolved has to guess which database to write to. Best
sequenced after the front-door work lands, so it gets a clear run rather than
competing with user-visible deadlines — but do not let it slide another
quarter.

**Next quarter.** C11, C12, C13, C14, C15, C16.

---

## 7. Verification checklist

Re-confirm before relying on any claim above:

- [ ] `git status` clean before edits start.
- [ ] `pytest` collects **and passes** in `app/backend/`, `knowledge-builder/`,
      and repo-root `tests/`.
- [ ] CI goes red when a test is deliberately broken (proves C1 closed).
- [ ] `cargo test` and `cargo clippy --deny warnings` pass in `mcp/`.
- [ ] `curl https://advandeb.com/api/health` returns JSON, not HTML.
- [ ] A refresh token sent as a bearer to `/api/users/me` returns 401.
- [ ] An anonymous `GET /` returns page content, not just the auth wall
      (proves C5 closed).
- [ ] `docs/knowledge_graph_scheme_descriptions.md` spot-checked against the
      live Arango schemas.
